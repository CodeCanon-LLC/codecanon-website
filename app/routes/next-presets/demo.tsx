import { NextPresetsDemo } from "@/apps/next-presets";

export function meta() {
  return [{ title: "Next Presets Demo — CodeCanon" }];
}

export default function Page() {
  return <NextPresetsDemo />;
}
